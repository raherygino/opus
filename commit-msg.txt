Improve Armement: code secret UI, reintegration date+GPS, signature in PDF

Android form UI:
- Code secret field and "Vérifier" button are now stacked vertically
  (top and bottom) instead of side by side in a Row
- Removed the eye icon that toggled code visibility — the code is
  always masked (PasswordVisualTransformation) with no toggle

Reintegration enhancements (all platforms):
- Added date_reintegration column (migration 029) alongside the
  existing heure_reintegration, so the full date+time is recorded
- Added reintegration_latitude and reintegration_longitude columns
  to capture where the weapon was returned (required on Android)
- API: Armement::reintegrate() and validateReintegration() accept
  the new fields; date_reintegration is required server-side
- Android: ReintegrationData, ReintegrationRequest, DTOs, domain
  model, and mapper all carry the new fields
- Android reintegration dialog:
  - Added date field (defaults to today)
  - Added GPS capture section with "Capturer ma position" button
    (required — reintegration is blocked without coordinates)
  - Added a Surface background for the GPS section to fix the
    transparent dialog appearance
  - Permission launcher requests fine/coarse location if not granted
- Android detail screen: shows date + coordinates of reintegration
  with a "Voir sur la carte" button that opens the native MapActivity
- Desktop detail: shows date + coordinates of reintegration with an
  inline OSM map toggle (same as perception location)
- Desktop PDF export: includes date_reintegration and reintegration
  coordinates in the Réintégration section

Signature in PDF export (desktop):
- The agent's signature_svg is now rendered in the exported PDF
- SVG is converted to PNG via canvas, then embedded with doc.addImage
- Falls back silently if SVG conversion fails

Verification:
- API tests: 92/92 passing (7 new reintegration checks)
- Desktop build: success
- Android compile: BUILD SUCCESSFUL

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
