Add agent identity verification and signature capture to Armement

Implements the full "Select Personnel -> Enter Code secret -> Verify ->
Confirm Weapon Handover -> Capture Signature" workflow across API,
Desktop, and Android.

Database:
- migration 026: personnel.code_secret_hash (bcrypt-hashed, never exposed)
- migration 027: armement.agent_verifie, agent_verifie_at, signature_svg

API:
- Personnel model: setCodeSecret/verifyCodeSecret/hasCodeSecret, strip
  code_secret_hash from all responses (getById, getAll, getAvailableForUser)
- Personnel controller: POST /code-secret, POST /verify-code-secret
  endpoints; code_secret accepted on create/update (hashed server-side)
- Armement controller: verifies code_secret server-side before creating
  a perception; stores agent_verifie/agent_verifie_at/signature_svg;
  verification + signature fields are one-way (not editable on update)
- Routes added in index.php
- ArmementTest extended: 76/76 checks passing (code secret set/verify/
  clear, hash stripped from responses, verification + signature
  persistence, update() cannot modify verification fields, reintegration
  preserves verification data)

Desktop:
- Personnel form: code secret field (set/replace, show/hide)
- Armement form: verification step (enter code -> verify -> badge) +
  signature capture via reused SignaturePadDialog; submit blocked until
  verified on create
- Armement detail: verification status badge + timestamp + SVG signature
  display; PDF export includes verification status

Android:
- Personnel DTO/model/mapper: has_code_secret field
- Armement DTO/model/mapper: agentVerifie, agentVerifieAt, signatureSvg
- ApiService: setPersonnelCodeSecret, verifyPersonnelCodeSecret
- PersonnelRepository: setCodeSecret, verifyCodeSecret
- ArmementFormData: codeSecret, signatureSvg
- Personnel form: code secret field with show/hide
- Armement form: verification step + SignatureCaptureDialog (local
  finger-drawing composable producing SVG)
- Armement detail: verification status + WebView-based SVG signature
  display

Verification:
- API tests: 76/76 passing
- Desktop build: success
- Android compile + unit tests: BUILD SUCCESSFUL

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
